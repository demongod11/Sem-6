int learn_Compilers(){
	return Learning;
}

void genrate_marks(){
	int t = learn_Compilers();
	t = t * 2;
}
int getMarks(){
	genrate_marks();
	return 100;
}
int main(){
	int n;
	int array[2000];
	int c;
	int a = array[0];

	for (int i = 0; i < n; i++){
		// a &=34;
		// running a loop
	}
	for (;;){
		//loop with empty
	}

	for (c = n - 1; c >= 0; c--){
		d += c;
	}

	int alpha = 67;
	int gandhi = 23;
	a = b + 45;
	int z = a;
	for (c = 0; c <= n - 1; c++){
		if(sajal == gandhi)
			return 100;
	}

	// checking operations
	int *x = &a;
	x->a;
	a = !a;
	a = a * c;
	a = a / b;
	a = a + b;
	a = a % 3;

	int b = a && c;
	int bb = b || (c == -c) && (!a);

	&a; *a; +a; -a; ! a;

	// This is single line comment
	int kitty = 1 * x + 6;
	int x = kitty;
	int world = 45;

	for (int toysssssss = 0; toysssssss < 45; toysssssss++){
		int dog = toysssssss; // animals
	}
}

void making_useless_functions(int no_work, int time_pass){
	return a1;
}